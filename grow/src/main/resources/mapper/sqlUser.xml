<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.master.grow.user.mapper.UserMapper">

<insert id="InsertNewUser">
	insert into user_data (user_id , user_name , user_email , user_password , user_repeat_password) values(#{userId},#{userName},#{userEmail},#{userPw},#{userRePw})
</insert>

<select id="SelectNewInsertUserIdCheck" resultType="int">
	select count(*)
	from user_data
	where user_id = #{userId}
</select>

<select id="userLoginEvent" resultType="int">
	select count(*)
	from user_data
	where user_id = #{userId}
	and user_password = #{userPw}
</select>


</mapper>